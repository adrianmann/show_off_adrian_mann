<% if @widgets.any? %>
  <table>
	  <tr>
	    <th>Name</th>
	  </tr>

	  <% @widgets.each do |widget| %>
	    <tr>
	      <td><%= widget["name"] %></td>
	    </tr>
	  <% end %>
	</table>
<% else %>
	<p>There were no widgets found.</p>
<% end %>
